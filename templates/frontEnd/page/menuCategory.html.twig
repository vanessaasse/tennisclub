<div class="list-pages">
{% for page in listPages %}
     {% if page.isPublished == true %}
         <i class="fas fa-angle-double-right"></i> <a href="{{ path('page.show', {'slug' : page.slug}) }}">{{ page
         .title }}</a><br/>
     {% endif %}
 {% endfor %}
</div>